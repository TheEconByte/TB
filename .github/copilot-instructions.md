# TrendBench repository instructions

Before editing, read `/AGENTS.md`, `/docs/PROJECT_CONTEXT.md`, and `/docs/TEAM_AI_WORKFLOW.md` in that order.

Do not duplicate their rules here. Treat those files as the current project, data, ownership, validation, and multi-agent collaboration contract. If they conflict with code or with each other, report the conflict instead of silently choosing one.
