# 생성물·의존성
.next/
node_modules/
src/generated/
test-results/
playwright-report/
coverage/
next-env.d.ts
package-lock.json

# 카탈로그 checksum은 파일 바이트의 SHA-256이다. 포맷이 바이트를 바꾸면 새 릴리스가 된다.
catalog/

# migration은 적용 후 수정하지 않는다.
prisma/migrations/

# 문서는 포맷 대상이 아니다. 한글 표 정렬이 전체 diff를 만든다.
*.md
