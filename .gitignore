# Node
node_modules/
dist/
app/src/generated/prisma/

# Next.js·테스트 산출물
.next/
out/
*.tsbuildinfo
coverage/
playwright-report/
test-results/
# next dev와 next build가 서로 다른 경로로 다시 쓰는 생성 파일. typecheck가 next typegen으로 만든다.
next-env.d.ts

# 환경파일. 예시(.env.example)만 추적한다.
.env
.env.*
!.env.example

# 공공 원본 데이터. 준비 방법은 docs/DEVELOPMENT.md의 로컬 데이터 준비를 따른다.
data/raw/

# Claude Code 개인 설정. 팀 공통 설정은 .claude/settings.json에 둔다.
.claude/settings.local.json

# 편집기·OS
.idea/
*.iml
.DS_Store
