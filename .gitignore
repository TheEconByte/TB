# Java / Spring
/build/
/.gradle/
*.class
*.jar

# IntelliJ
.idea/
*.iml

# Node
node_modules/
dist/
app/src/generated/prisma/

# Env
.env
.env.local

# OS
.DS_Store

# Upload files
uploads/
*.xlsx

# Next.js / local artifacts
.next/
out/
*.tsbuildinfo
# next dev와 next build가 서로 다른 경로로 다시 쓰는 생성 파일. typecheck가 next typegen으로 만든다.
next-env.d.ts

# Claude Code 개인 설정. 팀 공통 설정은 .claude/settings.json에 둔다.
.claude/settings.local.json
coverage/
playwright-report/
test-results/
.env.*
!.env.example
data/raw/
